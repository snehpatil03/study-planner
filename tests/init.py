"""
tests package

This package contains basic unit tests for the personalised learning assistant.
The tests use fake embeddings and a fake language model to avoid calling
external APIs.  To run the test suite install the requirements and execute
`pytest` from the repository root.
"""
